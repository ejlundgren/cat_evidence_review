# cat_evidence_review

The scripts are organized into two folders—one for pre-processing data and one for producing statistics/figures/etc. The scripts should be run sequentially to produce files in the 'builds' directory or 'figures' directory.
