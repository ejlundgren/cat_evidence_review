# cat_evidence_review

The scripts are organized into two folders—one for pre-processing data (scripts/1_data_mgmt/) and one for producing statistics/figures/etc (scripts/2_analysis/). Helper functions are in scripts/function.

The scripts should be run sequentially (based on numeric prefix) to produce files in the 'builds' directory or 'figures' directory.
